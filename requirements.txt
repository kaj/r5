# Required python packages.
# Can be installed py "pip install -r requirements.txt"

Django >= 1.9
django-autoslug
django-taggit
django_compressor
django-debug-toolbar
python-memcached
django-simplegravatar
IPy
Jinja2
pymongo
lxml
# Source higlighing for code examples
Pygments

# For testing
html5lib
cssselect

# Collect statistics in a nice way
# django-statsd-mozilla

gunicorn
setproctitle
