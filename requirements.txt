# Required python packages.
# Can be installed py "pip install -r requirements.txt"

Django >= 1.11, < 1.12
django-taggit
django_compressor
django-debug-toolbar
# Some bug makes 1.58 impossible to install on guran
python-memcached == 1.57
django-simplegravatar
IPy
Jinja2
lxml
psycopg2
pillow
# Source higlighing for code examples
Pygments

# For testing
html5lib
cssselect

gunicorn
setproctitle
